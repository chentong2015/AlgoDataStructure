TODO: Sliding windows 关于数组和字符串的滑动窗口算法
1. 所选的区间内的数据必须是连续Consecutive/Contiguous
2. 所选的"框选"区间必须是闭合区间[i, j]
3. 每读取一个新位置的值必须能够确定指针移动

for(item: items) {
    // update value
    while(condition) { // 特定的判断条件
         // 滑动坐标或者重新计算
    }
    // refresh result
}
