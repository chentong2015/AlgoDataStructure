TODO: Sliding windows Algo 关于数组和字符串的滑动窗口算法
1. 所选的区间内的数据必须是连续Consecutive/Contiguous
2. 所选的区间是[i, j]闭合的区间，用于"框选"一段数据的范围
3. 每读取一个新位置的值必须能够确定指针移动
   while(condition) { // 滑动窗口基于特定的判断条件
      // 滑动坐标或者重新计算
   }